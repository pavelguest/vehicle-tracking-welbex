import type { NativeStackScreenProps } from '@react-navigation/native-stack';
import { NavigationParams } from '../../navigation/Navigation.types';

export type TSettingsProps = NativeStackScreenProps<
  NavigationParams,
  'Settings'
>;
